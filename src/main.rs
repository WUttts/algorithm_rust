#![allow(unused)]
use algorithm_rust::mystruct::list::ListNode;
use std::collections::{BinaryHeap, HashMap, HashSet, LinkedList};
fn main() {
    let s = "{[()]}";
    for b in s.as_bytes() {
        println!("{}",b);
    }
}
